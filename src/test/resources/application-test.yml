spring:
  datasource:
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
    url: jdbc:tc:postgresql:12.4:///education
    username: postgres
    password: postgres

#  Конфигурация для проверки работы mappedBy (и его отсутствие) + работа ddl-auto
#  liquibase:
#    enabled: false
#  jpa:
#    hibernate:
#      ddl-auto: create-drop